// Renderer captures audio/video directly via getUserMedia/MediaRecorder,
// so no privileged APIs need to be bridged here yet. This file exists as
// the seam for future IPC (e.g. project persistence to disk).
export {};
